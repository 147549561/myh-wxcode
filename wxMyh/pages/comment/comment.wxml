<form bindsubmit="submitFeedback"> 
    <textarea placeholder="请写出您的评价(不少于7个字)" name="content" maxlength="300" auto-focus bindinput="bindinput"/>
    <view class="nubmer">{{num}}/300</view>
    <button formType="submit" class="edit-btn" disabled="{{disabled}}">提交</button>
</form>